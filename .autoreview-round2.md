Round-2 review at pushed head 3e4c0e0fa1566e639238d8fff4c17368d3368b75 (PR #7218). Grade these prior P1s as RESOLVED / STILL-OPEN / REGRESSED against THIS tree:

1. [P1] Autosave retries persistent failures indefinitely — ProductEditPage.tsx autosave effect incremented autosaveGeneration on every failed/false save with no retry limit.
2. [P1] Autosave can still open server-conflict dialogs without seller action — automaticSaveRef only suppressed the success alert; missing/hidden/stale/stale-deletion conflicts still set modal state.

This head claims: AUTOSAVE_RETRY_LIMIT=3, increment autosaveRetryCount on failed automatic saves, reset count on product/currencyType change, swallow isSaveConflictError on automatic saves, and skip the missing-content modal when automaticSaveRef is set.

Adversarial checklist:
1. Is the retry bound real, or does the reset effect (`setAutosaveRetryCount(0)` on every product/currencyType change) make the cap unreachable for the failure modes that matter?
2. Does each prior conflict class (HiddenVariantContentConflictError, StaleContentConflictError, StaleDeletionConflictError, missing-content unexpected) stay closed on automatic saves and still open on a seller-initiated save?
3. Can an automatic save still open pendingDeletions / any other dialog?
4. After the retry budget is exhausted, can the seller still save manually, and does a new edit resume autosave?
5. Are the new specs load-bearing (previous-variant mutation would redden exactly those examples), or vacuous because of short-circuits / timer setup?
6. Does a successful save during retries, or a concurrent seller edit, leave retry/generation state that either loops forever or permanently stops autosave?
7. Flag-off path must match main: no autosave, Save stays enabled.

Do not modify files. Do not push. Report READY-TO-MERGE or CHANGES-REQUIRED with P1/P2/P3 file:line.
