Review #7686 at HEAD against origin/main. Do not commit, push, merge, or post comments. You are in an isolated review tree; never modify other worktrees.
Check: (1) legacy US llc renders unselected and cannot submit until a supported member count is chosen; selection and save use the same active list. Unknown codes and country-specific/generic lists must agree. (2) US structure create/update and clearing structure on individual switch use legal-entity country, never residence. Trace country_code's assignment in _update_account. (3) LLC tax export mappings and presenter props remain complete. (4) Existing generic values must not be guessed into a different legal classification. Non-profit mapping is explicitly not extended here. (5) new specs must fail on reverted validation; author measured 22/22 Vitest passing after main sync, validation-only mutant 1/11 failing. (6) Code comments concise, no production PII/media in git. Real Rails+browser captures: after desktop1440/mobile375 × light/dark, placeholder and selected multi-member verified; before origin/main capture is complete and all 12 screenshots plus a captured-state walkthrough were attached and download/hash verified. Check rendered UI behavior through code/tests; do not call evidence missing merely because PNGs aren't in git (must never be committed). (7) No auto-merge: human payout-setup handoff only. AE residence gate intentionally unchanged: Stripe docs state company.structure optional, company.vat_id absent from AE mandatory fields; 22 current AE-company/non-AE-residence accounts, 18 live Stripe, audited read-only replica lag1s. That is a tracker decision, not part of this PR diff.

Current head includes a main sync to resolve an append-only conflict in Show.test.tsx. Both Egyptian bank-code tests and our legacy-business-type tests survived; 22 Vitest tests pass. Two browser specs pass (saved-EIN edit using a supported type, and legacy LLC rejection then explicit reselection). Review this exact head; do not reuse an old marker.
