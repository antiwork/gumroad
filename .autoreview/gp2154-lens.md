Domain lens for seller-scoped call booking capacity (antiwork/gumroad gp#2154, PR #7250).

This diff serializes call-slot occupancy across ALL call products on one seller: validation, availability display, and the checkout inventory lock. Money path: Purchase::CreateService. A later commit keeps daily max counted from the product's own sold calls.

Numbered checks:
1. Occupancy query vs association: Call#seller_occupied_calls uses Call.occupies_availability.where(purchases: { seller_id: }). occupies_availability already joins(:purchase). Confirm the hash-where hits that join, not a missing-table error. User#sold_calls is through: :sales. Confirm sales.seller_id is the same identity as link.user_id (collaborators / team members / affiliate sellers).
2. Inventory lock completeness: call products always take the lock (allow_parallel_purchases? excludes NATIVE_TYPE_CALL). The new lock is per seller_id. Two buyers booking the SAME wall-clock slot on TWO products of one seller must serialize. Two buyers booking DIFFERENT slots on two products now also serialize (blast-radius / contention). Name whether that is acceptable. Confirm gift sender+receiver and bundle/cart combined charges still share one lock or can double-book.
3. Daily max vs seller calendar: ComputeCallAvailabilitiesService now hides seller-wide occupied intervals but counts maximum_calls_per_day from product.sold_calls. CallLimitationInfo#can_take_more_calls_on? still counts product.sold_calls. Confirm both display and checkout-validation daily-max paths stay product-scoped, and that seller-wide occupancy cannot exhaust another product's daily cap. Grep sold_calls / occupies_availability / call_start_time across app/lib. Any remaining product-scoped occupancy that still double-books a wall-clock slot is in-scope.
4. Occupies-availability membership: in-progress, not-charged, successful, gift sender vs receiver, fully refunded. Confirm seller-scoped queries inherit occupies_availability (not raw sold_calls). A successful call on product A must block product B; a fully refunded one must not.
5. Specs: model spec for cross-product rejection and compute-availabilities specs exist. Is there a spec that the checkout lock key is seller-scoped? A mutation that keeps seller_call_inventory but keys it on product.id (or always uses product_inventory for calls) — would the suite stay green? If yes, that is a coverage hole on the money path.
6. Self-collision: seller_occupied_calls on create of the current purchase — is the in-progress purchase already persisted so the new call sees itself as occupying the slot?
7. Time/overlap: overlaps_with is start < other_end AND end > other_start. Adjacent slots (end == next start) must still be allowed after the seller-scope change.
8. Do not run the test suite. Static review only. Verdict READY-TO-MERGE or CHANGES-REQUIRED with P0/P1 file:line.
