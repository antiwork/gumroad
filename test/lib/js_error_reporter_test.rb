# frozen_string_literal: true

require "test_helper"

class JSErrorReporterTest < ActiveSupport::TestCase
  test "pending direct Playwright conversion" do
    skip "Pending direct Playwright conversion after removing the legacy browser driver"
  end
end
