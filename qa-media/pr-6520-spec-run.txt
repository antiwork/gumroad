$ bundle exec rspec spec/business/payments/merchant_registration/stripe/stripe_merchant_account_manager_spec.rb -e "update_person"

StripeMerchantAccountManager
  .update_person
    list_persons filters by relationship.representative on Stripe's side
      queries Stripe with relationship: { representative: true }, limit: 1 — no client-side scanning
    with the representative on the Stripe account
      sends only representative: true and lets Stripe preserve owner/title/percent_ownership set via BeneficialOwnersSection
    when Stripe returns no persons for the account
      returns early without calling update_person
    individual→company transition
      seeds owner: true, title, percent_ownership: 100 when nobody else owns any of the company
      claims only the ownership the existing beneficial owners have not, so Stripe does not reject the switch for exceeding 100 percent
      rounds the claimed share DOWN so a fractional remainder cannot push the company over 100 percent
      treats an owner with no recorded share as owning nothing
      claims nothing when the recorded owners already add up to more than the whole company
      uses the ownership figure the caller already read instead of asking Stripe again
      claims no ownership at all when the existing beneficial owners already account for the whole company
      ignores the representative's own existing ownership share when working out what is unclaimed

Finished in 20.62 seconds (files took 12.92 seconds to load)
11 examples, 0 failures


RED CHECK — with the fix reverted (old `percent_ownership: 100` restored), the same specs fail:

  1) ... claims only the ownership the existing beneficial owners have not ...
  2) ... claims no ownership at all when the existing beneficial owners already account for the whole company
     Failure/Error: expect(captured_attributes[:relationship]).not_to have_key(:owner)
  3) ... ignores the representative's own existing ownership share when working out what is unclaimed
     Failure/Error: expect(captured_attributes[:relationship][:percent_ownership]).to eq(66.67)

  4 examples, 3 failures


RUBOCOP
$ bundle exec rubocop app/.../stripe_merchant_account_manager.rb spec/.../stripe_merchant_account_manager_spec.rb
2 files inspected, no offenses detected
