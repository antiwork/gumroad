RepairOrderChargeOutcomesJob — late-settling orders reached (Greptile P1, comment 3703398487)
Tree: ~/work/pr6907-repair-window, head 346449f79b + spec fix. Seeded test DB.

GREEN
  $ bundle exec rspec spec/sidekiq/repair_order_charge_outcomes_job_spec.rb
  6 examples, 0 failures

MUTANT 1 — drop the backlog pass entirely (revert to the reported 3-day created_at window):
  (recent_candidate_ids + backlog_candidate_ids).uniq  ->  recent_candidate_ids
  6 examples, 2 failures
    :37 flags an order that settled long after checkout, past the freshness window
    :69 resumes the backlog walk from the saved cursor and wraps once past the end
  This is the reported defect, reproduced: a 30-day-old order holding successful + failed
  line items stays unflagged forever.

MUTANT 2 — drop the BACKLOG_HORIZON lower bound (unbounded walk):
  created_at: BACKLOG_HORIZON.ago...RECENT_WINDOW.ago  ->  created_at: ...RECENT_WINDOW.ago
  6 examples, 1 failure
    :46 leaves an order older than the backlog horizon alone

MUTANT 3 — never advance the cursor (walk wedges on its first page):
  delete `save_cursor(ids.last) if ids.any?`
  6 examples, 1 failure
    :69 resumes the backlog walk from the saved cursor and wraps once past the end

MECHANISM PROBE (throwaway spec, one 30-day-old partial order, MAX_BACKLOG_SCANNED default):
  RECENT=[]            # the freshness pass cannot see it — the reported gap
  PAGE=[288]           # the backlog page does
  BACKLOG=[288]
  CURSOR_AFTER="288"
  ORDER_ID=288

A first version of the wrap assertion expected the cursor back at `older.id` after the third
run and FAILED (Expected 0 to eq 222). The code was right and the assertion wrong: by then both
orders are flagged, so they have left the candidate set and the wrapped page is legitimately
empty. The example now asserts 0, which is what lets the next lap pick up a newly stranded order.
