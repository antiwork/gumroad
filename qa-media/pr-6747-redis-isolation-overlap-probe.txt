Captured output of qa-media/pr-6747-redis-isolation-overlap-probe.sh on this branch, against a
Redis started with 64 databases (`redis-server --databases 64`) on port 6399.

The pairing is a run that HOLDS a lease against a run that SKIPPED leasing and therefore
still uses — and flushes — the .env.test databases 10-13. Slot 0 is held by the probe so
the leased writer lands on a later block, which is where the original arithmetic overlapped.

== BEFORE — slots dealt from database 4 upward (the arithmetic the pre-merge review caught)

  slot 0 held by the probe, so the writer leases a later block
  flusher  fallback databases [10, 11, 12, 13] (no lease held)
  flusher  done
  writer   leased databases [8, 9, 10, 11]
  writer   WIPED databases [10, 11] — flushed by a run holding no lease on them

The writer held a valid lease on slot 1 and was told it was isolated. Two of its four
databases were the fallback run's, so its keys were erased mid-run with nothing to point
at — the same race the branch set out to fix, made harder to see by the lease.

== AFTER — floor derived from the *_REDIS_HOST values (TestRedisIsolation.reserved_databases)

  slot 0 held by the probe, so the writer leases a later block
  flusher  fallback databases [10, 11, 12, 13] (no lease held)
  flusher  done
  writer   leased databases [18, 19, 20, 21]
  writer   SURVIVED — no leased database was flushed by the fallback run

The floor is now 14 (one past the highest index the env vars name), so every leased block
sits above every database any fallback run touches.
