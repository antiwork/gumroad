Live spring preload-and-fork run at the head of fix/per-run-test-redis-databases.
Redis started with --databases 64 on port 6399; RAILS_ENV=test.

  $ bin/spring stop
  $ bin/rails runner 'nil'            # warms the spring server, which claims the lease
  [test-redis-isolation] slot 3 → Redis databases 26, 27, 28, 29 on localhost:6399
  Running via Spring preloader in process 64885

  $ ( bin/rails runner 'sleep 6' & ) ; sleep 3 ; bin/rails runner 'puts ENV["REDIS_HOST"]'

  --- first command
  Running via Spring preloader in process 64924

  --- second command, overlapping the first
  B redis=localhost:6399/26
  Running via Spring preloader in process 65159
  [test-redis-isolation] 2 test commands are sharing Redis databases 26, 27, 28, 29 (pids 64924, 65159).
  [test-redis-isolation] Spring preloads the app once per checkout and forks each command from it, so they inherit one lease and will flush each other's keys mid-test. Run them with DISABLE_SPRING=1, or from separate checkouts.

Before this change the second command printed nothing: it used databases 26-29 alongside
the first and flushed them, with no way for either to know.

The first command is silent, so the warning is not unconditional noise.

Guards proven load-bearing by reverting each and re-running the spec file:

  no liveness filter on sibling pids  → reddens "does not warn about a command killed
                                        before it could deregister"
  at_exit never deregisters           → reddens "removes the command from the lease when
                                        it exits"
  warn block removed                  → reddens "warns the second command that it is
                                        sharing the first command's databases"
  no TTL on the commands hash         → reddens "gives the commands hash the lease's TTL
                                        so it cannot outlive the block"

  $ bundle exec rspec spec/config/test_redis_isolation_spec.rb
  31 examples, 0 failures
  $ bundle exec rubocop config/test_redis_isolation.rb config/spring.rb spec/config/test_redis_isolation_spec.rb
  3 files inspected, no offenses detected
