UPDATE (parent, 8/5 ~05:25): Sahil spotted that branch CI green was PARTIAL-suite only (Test Relevant). I added the run-all-specs label + pushed empty commit 8d406485a — FULL suite now running on the branch. Do NOT merge until THAT run is green. Your local 7/7 red may be real, not artifact — the full-suite run decides. This also explains #7070: branch-green (partial) vs main-red (full).
PARENT (05:42): Your local 7/7 red is the KNOWN routes.js artifact — app/javascript/utils/routes.js line 120 has host :31337 baked in, but your lane serves on a different port, so buy-button links go nowhere. Fix: under the SAME lane env run `RAILS_ENV=test bundle exec rails js:export` then rebuild vite test assets, THEN rerun. Do not chase app code for this. Your commit 25c982d47 (confirm-response keying) may be real — but verify it's needed with correct assets before keeping it; if the revert is faithful and green with fixed assets, prefer dropping extra commits from a pure revert PR (revert of that commit, not rebase — history is pushed).
