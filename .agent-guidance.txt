DIRECTION FROM SAHIL (8/5, DM) — UPI Autopay (#6738) vs the scoped CardElement gate:

UPI Autopay should use the PAYMENT ELEMENT if possible. If not possible (Element confirm path still being tested/fixed — gumroad-private#1853), CardElement is acceptable BUT the flow must stay multi-currency: INR presentment for India etc.

Implication for the presenter conflict resolution: the scoped CardElement gate (stripe_payment_presenter.rb:78, `sca_or_mandate_confirm_flow`) routes ALL recurring carts to card_element. UPI cannot be collected by CardElement at all — a card-only surface removes the UPI option for Indian buyers entirely. So #6738's INR/UPI-eligible recurring carts need a carve-out AHEAD of the gate: they keep the Payment Element (with INR presentment + UPI payment method), and only card-paying mandate carts fall through to the CardElement lane. The known Element confirm crash was in confirmCardPayment card flows; #6738's UPI confirm path is its own machinery and must be verified green on top of current main (tracked: gumroad-private#1856). If UPI-on-Element proves broken in testing, fallback per Sahil is CardElement for the card part but INR multi-currency must be preserved either way.
